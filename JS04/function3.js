// Example 1
function add(a, b) {
    return a + b;
  }
  
  let result = add(5, 3);
  console.log(result);

  
// Example 2
let multiply = function (x, y) {
    return x * y;
}

let final = multiply (5, 2);

console.log (final);